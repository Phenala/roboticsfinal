<launch>
  <!-- We resume the logic in empty_world.launch, changing only the name of the world to be launched -->
  <include file="$(find assignment_arm)/launch/empty_world.launch">
    <arg name="world_name" value="$(find assignment_arm)/worlds/plane.world"/>
    <!-- more default parameters can be changed here -->
  </include>
</launch>